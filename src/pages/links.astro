---
import { CLIENT } from "../config/client";
import Layout from "../layouts/Layout.astro";
import LinksBoard from "../components/LinksBoard.astro";
// Pagina "Links" (QR code). Rebrand Chouchou: fondo bianco + halo magenta,
// selettore FR|EN, bottoni (Réserver, Carte, Agenda, Avis avec notation),
// social. Contenuto condiviso in LinksBoard (fr + en).
---

<Layout bare={true} title={`Links — ${CLIENT.nome}`}>
  <LinksBoard />
</Layout>
