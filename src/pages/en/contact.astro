---
// Version EN de /contact : rend la page FR (bilingue) avec lang=en (détectée par
// l'URL /en/...). Source unique par page, pas de duplication.
import FrPage from "../contact.astro";
export const prerender = false;
---

<FrPage />
