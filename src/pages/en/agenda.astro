---
// Version EN de /agenda : rend la page FR (bilingue) avec lang=en (détectée par
// l'URL /en/...). Source unique par page, pas de duplication.
import FrPage from "../agenda.astro";
export const prerender = false;
---

<FrPage />
